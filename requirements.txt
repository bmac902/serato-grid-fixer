# Serato Grid Fixer — Dependencies

# UI automation (mouse clicks)
pyautogui>=0.9.54

# Global hotkey detection (the ~ key)
keyboard>=0.13.5

# Mouse click capture (used by calibrate_buttons.py)
pynput>=1.7.6
